#include <stdio.h>
//sizeof is an operator not a function - tells you how many bytes of memory are used by a given type

//sizeof(int) = number of bytes occupied by a variable of type int

//casting - typically go from more precise to less precise in calculations - ie floats to ints 
// asterik * is an operator that represents a pointer to a variable ex: *a 
// ? used for comparisons: if condition is true ? then value X: otherwise value Y 

// operator precedence
